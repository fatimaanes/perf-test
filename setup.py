#!/usr/bin/env python3
from setuptools import setup, find_packages

setup(
    name="demo-benchmarks",
    version="0.1.0",
    description="Demo project for ASV benchmarking",
    packages=find_packages(),
    python_requires=">=3.6",
)